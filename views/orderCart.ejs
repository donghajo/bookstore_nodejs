<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRk2vvoC2f3B09zVXn8CA5QIVfZOJ3BCsw2P0p/We" crossorigin="anonymous">
    <title>Cart</title>
</head>
<body class="text-center" style="margin-top: 30px; margin-left: 50px; margin-right: 50px;">
    <div class="px-4 py-5 my-5 text-center">
      <header>
        <h1 class="display-5 fw-bold"><%=session%>님의 주문</h1>
      </header>
      <br>
      <form action='/orderList/<%=session%>' method="post">
        <div class="col-lg-10 mx-auto">
          <table class="table">
            
            <thead>
              <tr>
                <th scope="col">번호</th>
                <th scope="col">책정보</th>
                <th scope="col">가격</th>
                <th scope="col">수량</th>
                <th scope="col">총액</th>
                <!-- <th scope="col"></th> -->
              </tr>
            </thead>
            <tbody>
              <%
              for(var i = 0; i<cart.length; i++)
              {
              %>
              <tr>
                <td><%=i+1%></td>
                <td><%=cart[i].book_id%></a></td>
                <td><%=cart[i].cart_book_price%></td>
                <td><%=cart[i].cart_book_quantity%></td>          
                <td><%=cart[i].cart_book_quantity * cart[i].cart_book_price%></td>
                <!-- <td><form action="/cart/delete/<%=session%>/<%=cart[i].book_id%>" method="post"><button type="submit" class="btn btn-dark ">삭제</button></form></td> -->
              </tr>
              <%
              }
              %>
            </tbody>
          </table>
          <br>
          <select name="card">
            <%
            for(var i = 0; i<card.length; i++)
            {
            %>
              <option value="selectCard"><%=card[i].card_kind%>/<%=card[i].card_id%></option>
            <%
            }
            %>
          </select>
            <select name="address">
            <%
            for(var i = 0; i<address.length; i++)
            {
            %>
              <option value="selectAddress"><%=address[i].address_default%>/<%=address[i].address_detail%></option>
            <%
            }
            %>
          </select>

          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <button type="button" class="btn btn-dark btn-lg px-4 gap-3">주문</button>
          </div>
        </div>
      </form>
    </div>
</body>
</html>